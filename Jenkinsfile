pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                sh 'echo "Hello Test World"'
                sh 'touch /tmp/test.msg'
            }            
        }
    }
}
